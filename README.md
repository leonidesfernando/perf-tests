# PERF-TESTS
This is a simple project to group some scritps form `JMeter` and `LightHouse` to run against a demo app **_Shopizer_** locally.

## How to run
Inside each folder project you can find a specific *README* with instructions to execute these scritps.