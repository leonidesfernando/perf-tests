# BACKEND JMeter Perf Test

## REQUIREMENTS 
To run this JMeter script will be necessary to install the plugin Parametrized Controller, from Plugins Manager.

## How to execute
To run the JMeter script just execute the `runAndGenerateReport` and the report will be generated.